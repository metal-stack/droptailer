syntax = "proto3";

package dropsink;

import "google/protobuf/timestamp.proto";

service DropSink {
    rpc Push (DropEntry) returns (Void) {};
}

message DropEntry {
    google.protobuf.Timestamp timestamp = 1;
    map<string, string> fields = 2;
}

message Void {}